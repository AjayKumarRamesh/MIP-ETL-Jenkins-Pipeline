pipeline {
    agent { label 'kube_pod_slave' }
    stages {
        /*stage('GitHub Checkout') {
            git credentialsId: 'sangita_id_rsa' ,
            url: 'ssh://git@github.ibm.com/CIO-MAP/Jenkins_Poc_mapetl.git', branch: 'master'  
        }*/
        stage('Kickoff Jobs') {
            steps {
                build job: 'MIP_DEV_Ruby_Pipeline', propagate: true, wait: false
                build job: 'MIP_DEV_DRUPAL_PIPELINE', propagate: true, wait: false
                build job: 'MIP_DEV_ADHOC-XREF_PIPELINE', propagate: true, wait: false
            }
        }
    }
}